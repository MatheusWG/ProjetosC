#include <stdio.h>
#include <windows.h>
#include <math.h>
#include <stdlib.h>

int main()
{
	int OP;
	double RET,REP;
	
	while(1)
	{
		printf("1. RETIRAR\n");
		printf("2. REPOR\n");
		scanf("%d",&OP);
		if(OP==2)
		{
			printf("Q");
		}
	}
}